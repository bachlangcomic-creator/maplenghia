name: Trigger V9.7.0 Publish Once

on:
  push:
    branches:
      - release-v9.7.0-detection-watchdog-parity

permissions:
  actions: write
  contents: read

jobs:
  dispatch-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch publish workflow with publish=true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run publish-v9.7.0.yml \
            --repo bachlangcomic-creator/maplenghia \
            --ref release-v9.7.0-detection-watchdog-parity \
            -f publish=true
