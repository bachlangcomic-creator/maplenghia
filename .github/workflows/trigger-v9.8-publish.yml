name: Trigger V9.8.0 Publish Once

on:
  push:
    branches:
      - release-v9.8.0-exact-detector-watchdog-recovery
    paths:
      - sync/v9.8-publish-ready.txt

permissions:
  actions: write
  contents: read

jobs:
  dispatch-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch publish workflow with publish=true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run publish-v9.8.0.yml \
            --repo bachlangcomic-creator/maplenghia \
            --ref release-v9.8.0-exact-detector-watchdog-recovery \
            -f publish=true
