# Nghia V9.9.0 — Forensic Parity Design

Date: 2026-09-12
Branch: `release-v9.9.0-forensic-parity`
Base: `release-v9.8.1-hold-combo-parity`

## Goal

V9.9.0 narrows the remaining behavior gap between Nghia and the supplied Spotify reference by implementing only behavior supported by Direct or High-structural forensic evidence. It must not invent missing timing, thread topology, detector parameters, or assets.

The five scoped parity areas are:

1. ONL/disconnect detection and reaction.
2. Auto-sell randomized scheduling.
3. `main_farm_worker` ownership/order reconstruction.
4. Pirate Den and Pirate Den 1 Hit micro-timing/state parity.
5. All Cure external-asset gateway and recovered market workflow readiness.

## Evidence Policy

Every V9.9 behavior added from Spotify must be recorded in evidence JSON with one of these labels:

- `direct`: literal asset, constant, function/symbol, threshold, coordinate, string, or timing present in the Spotify binary/payload.
- `high_structural`: control/data-flow reconstruction strongly supported by adjacent constants, locals, symbols, call relationships, and prior runtime observations, but not source-identical Python.
- `nghia_fallback`: compatibility or safety behavior added by Nghia and not attributed to Spotify.

V9.9 must not relabel a High-structural reconstruction as Direct.

Reference source hashes remain:

- Spotify core executable SHA-256: `6816a9acbf3feb4aa12da996d309844c7335f0fc6726dacbc4b78c94306adbed`
- `main.dll` SHA-256: `62860aeb8a80ac4a768aa8f3afafe23f079aa07e2981786e44989ad71b941cb0`
- `maps.json` SHA-256: `52364dc3a284e051a04263b49e008caf699de9127cc62c8c59b7a8bb6533671d`

## Architecture

V9.9 preserves the V9.8.1 subsystem boundaries instead of creating speculative Spotify threads. Five parity changes remain isolated behind existing modules and shared evidence contracts.

### 1. Disconnect / ONL

`spotify_detection_parity.py` gains the recovered ONL detector contract:

- exact asset: `ONL_B64`
- threshold: `0.95` (`direct`)
- watchdog-owned scan path

`spotify_watchdog.py` owns reaction handling. On a matched disconnect event:

1. release Nghia-owned inputs;
2. pause farming;
3. emit local alarm/status;
4. use the existing Telegram disconnect path when configured;
5. debounce repeat detections so one disconnect state does not spam alerts.

V9.9 does not reconnect, relogin, click session/login UI, or alter Nghia session/license behavior.

The exact frame/crop ownership is recorded as `high_structural` unless additional direct evidence proves a dedicated ROI. V9.9 must not invent one.

### 2. Auto-sell Jitter

`SpotifyWatchdogWorker.get_next_sell_delay()` changes from fixed `minutes * 60` to the recovered High-structural formula:

```python
low_minutes = max(10.0, sell_interval_minutes - 10.0)
high_minutes = sell_interval_minutes * 1.2
delay_seconds = random.uniform(low_minutes, high_minutes) * 60.0
```

Evidence classification:

- `10`, `10`, `max`, `1.2`, `random.uniform`, `60`: `direct`
- the operator tree/formula above: `high_structural`

Behavioral rules:

- a new randomized delay is generated when scheduling begins;
- after a successful sell, `last_sell_time` resets and a new randomized delay is generated;
- disabling auto-sell clears any stale next-delay value;
- scheduling failure never forces a sell action.

### 3. Main Farm Tick Orchestration

V9.9 does not create a new `main_farm_worker` thread. Instead, it reconstructs Spotify ownership/order as a tick orchestrator invoked from Nghia's existing main farm loop.

Recovered stage order:

1. pause / bot-state gate;
2. auto-sell / safe-place stage;
3. pet stage;
4. lost-player recovery stage;
5. Buff 1 stage;
6. Buff 2 stage;
7. farm-mode dispatch.

Each stage remains implemented by its current subsystem. The orchestrator owns ordering and short-circuit decisions only.

Recovered timing/state evidence to preserve includes:

- generic loot: base ~90s with `0.9..1.1` jitter (~81..99s);
- pet next interval: `600..800s`;
- pet hold: `500..1000ms`;
- buff press: `600..900ms`;
- buff base: ~120s with recovered jitter constants;
- lost-position staged recovery: approximately `0.1 / 1.5 / 3.0s`;
- fall confirmation: approximately `2.5s`;
- recovery action: `200..600ms`;
- reset delay: `20..40s`;
- floor-jump delay: `90..120s`.

Only timings whose current evidence file assigns `direct` or `high_structural` may be used in code.

Thread start order among watchdog/focus/main farm is not reconstructed. `session_monitor_worker` remains excluded from Nghia.

### 4. Pirate Den Parity

V9.9 preserves V9.8.1's shared hold-combo behavior and only updates timing/state branches supported by new forensic evidence.

#### Pirate Den normal

FARM preserves:

- shared TP+Skill combo window `0.200..0.500s`;
- fall confirm approximately `2.5s`;
- recovery action `0.200..0.600s`;
- recovery release settle around `0.01s` where supported;
- human jump branch probability `0.08`;
- jump duration `0.150..0.300s`;
- turn settle `0.05s`.

Bottom loot preserves:

- interval ~`81..99s`;
- descent settle `0.5..0.8s`;
- alternating sweep direction through `curr_loot_dir`;
- shared sweep combo `1.6s`.

Top loot preserves the recovered route and timing chain around `(135,177)` and return `(114,172)`, including the recovered cast/settle/Teleport/Skill windows where evidence is Direct or High-structural.

#### Pirate Den 1 Hit

FARM remains edge-driven one-hit behavior and must never call the shared spam combo helper.

Preserved semantics include:

- right setup at `(178,183)`;
- right-side spawn wait `0.300..1.500s`;
- left-side spawn wait `1.5s`;
- one hit per side transition;
- stuck timeout `1.5s`;
- fall confirm approximately `2.5s`;
- recovery `0.200..0.600s`.

Bottom loot keeps the shared `1.5s` sweep combo and fixed one-hit-specific route behavior. Top loot preserves its one-hit-specific route to `(135,177)` and return `(114,172)`.

Dynamic Combo remains dormant/evidence-gated. Pirate Laptop remains out of scope.

### 5. All Cure External Asset Gateway

V9.9 keeps All Cure fail-closed by default but adds a future-proof external asset gateway.

Asset directory:

`user_data/all_cure_images/`

Required filenames:

- `empty.png`
- `npc_all_cure.png`
- `all_cure_line.png`
- `confirm_all_cure.png`
- `buy_all_cure.png`
- `finish_buying.png`
- `shop.png`
- `exit.png`
- `all_cure_pots.png`
- `set_sell_item.png`
- `confirm_set_item_to_sell.png`
- `complete_set_item.png`

Gateway readiness requires all 12 files to exist and decode as valid PNG images. Missing, corrupt, non-PNG, or undecodable files block the transaction and produce a clear diagnostic without any market click.

The updater must never overwrite or delete `user_data/all_cure_images/`.

When the gateway is READY, the recovered Buy/Sell workflow may run using the external assets. It must not synthesize substitute images or fall back to coordinate-only clicking.

Recovered workflow evidence includes:

- buy quantity `100`;
- `TAB_USE_MARKET=(115,543)`;
- average price position `(766,397)`;
- buy-to-sell delay `1.0s`;
- failure retry `2.0s`;
- recovered per-step thresholds/timings only where classified Direct/High-structural in V9.9 evidence.

## Error Handling

V9.9 follows two rules:

- farm/scheduler paths are fail-soft: log the current error, release owned input if necessary, and skip the unsafe action/tick;
- market transactions are fail-closed: any missing evidence, asset-read failure, invalid PNG, detector error, or state mismatch blocks clicking.

Pirate input exceptions must release Nghia-owned inputs before returning control to the farm loop.

## Protected Behavior

V9.9 must not change:

- V9.8.1 shared hold-combo semantics for C1/C2/B1/B3;
- MiuMiu image-driven flow except where orchestrator ordering invokes it;
- CAPTCHA policy: detection/pause/alert only, no solver/bypass;
- DEAD/revive detector/action contract;
- login/license/session exclusion in Nghia;
- launcher update safety/rollback semantics;
- Dynamic Combo caller status;
- Pirate Laptop scope.

## Test Design

Implementation follows TDD.

### Disconnect tests

- exact `ONL_B64` path;
- threshold exactly `0.95`;
- match causes release -> pause -> alert reaction order;
- Telegram path only when configured;
- debounce prevents repeated alerts for one persistent disconnect;
- non-match leaves bot state unchanged.

### Sell scheduler tests

- `low=max(10,m-10)`;
- `high=m*1.2`;
- `random.uniform(low,high)*60` unit conversion;
- successful sell resets time and regenerates delay;
- disabling auto-sell clears stale schedule;
- evidence class for formula is High-structural.

### Main farm orchestrator tests

- exact stage order: state -> sell/safe -> pet -> recovery -> buff1 -> buff2 -> dispatch;
- stage short-circuit rules;
- existing subsystem implementations remain isolated;
- no `session_monitor_worker` introduced;
- no speculative worker-start-order assertion.

### Pirate tests

- all V9.8.1 hold-combo regressions remain green;
- normal FARM timing/state evidence;
- normal bottom/top loot timing/route evidence;
- 1 Hit FARM never calls shared spam combo;
- 1 Hit right/left spawn waits preserved;
- 1 Hit bottom/top loot remains one-hit-specific;
- input exception releases owned keys.

### All Cure tests

- all 12 valid PNGs -> READY;
- any one missing -> BLOCKED;
- wrong extension -> BLOCKED;
- corrupt PNG -> BLOCKED;
- decode failure -> BLOCKED;
- BLOCKED state performs zero market clicks;
- updater preserves `user_data/all_cure_images/`.

## Verification and Release Gate

Private preflight must run:

1. all V9.7 tests;
2. all V9.8 tests;
3. all V9.8.1 tests;
4. all V9.9 tests;
5. `compileall`;
6. static guards and evidence consistency checks;
7. Windows launcher smoke;
8. Portable/Update package member verification;
9. SHA-256 verification of generated artifacts.

Private preflight must not modify `main/latest.json`.

Public release version is `V9.9.0`. Public publishing requires explicit user approval (`publish V9.9.0`). After publishing, public assets must be re-downloaded and their SHA-256 values verified. Only then may `latest.json` be updated, and it must be updated last.

## Acceptance Criteria

1. ONL uses exact `ONL_B64` and threshold `0.95` through the watchdog path.
2. Disconnect reaction releases input, pauses farm, alerts locally, and debounces repeated detection.
3. Auto-sell scheduling uses the documented High-structural randomized formula and never mislabels it Direct.
4. Main farm ownership/order is reconstructed without inventing Spotify thread topology.
5. Pirate normal and Pirate 1 Hit apply only evidenced micro-timing/state changes while retaining V9.8.1 combat semantics.
6. Pirate 1 Hit FARM never becomes TP+Skill spam.
7. All Cure uses `user_data/all_cure_images/`, requires all 12 valid PNGs, and is fail-closed otherwise.
8. Updater preserves external All Cure assets.
9. Session/login/license behavior remains unchanged and `session_monitor_worker` is absent.
10. Dynamic Combo remains dormant/evidence-gated for target farm callers.
11. Pirate Laptop remains out of scope.
12. V9.7/V9.8/V9.8.1/V9.9 regression suites, compileall, static guards, Windows smoke, and private packaging verification all pass before public approval is requested.
13. No public V9.9.0 release is created without explicit user approval.
